To install the requirements,

cd YOLOv6
pip install -r requirements.txt 

------------------------------------------------------------------------------------------------------------------
To run, execute the following : 

cd YOLOv6
./script.sh

------------------------------------------------------------------------------------------------------------------
If you want to test custom photos, add them to data/images (inside YOLOv6 directory). The results are stored in 
runs/inference/exp . 